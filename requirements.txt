# Core (CPU-friendly, no GPU required) — enough to run tests + severity + prompting
numpy>=1.24
scipy>=1.10
scikit-image>=0.21
opencv-python-headless>=4.8
pillow>=9.5
omegaconf>=2.3
tqdm>=4.65

# Training (uncomment / install on Kaggle/Colab where torch is preinstalled)
# torch>=2.1
# torchvision>=0.16
# albumentations>=1.3

# Optional experiment tracking (no-op fallback if absent)
# wandb>=0.16

# Optional foundation backbone
# git+https://github.com/facebookresearch/sam2.git
